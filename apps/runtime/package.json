{
  "name": "@0xhoneyjar/activities-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin Hyper HTTP runtime skin over the activities Effect engine — serves the READ plane (health, beacon, activity-supertype reads) backed by the Seam-B Postgres adapters. Composition root for a deployable activities.0xhoneyjar.xyz.",
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "bun --hot src/server.ts",
    "typecheck": "tsc --noEmit",
    "openapi": "bun src/emit-openapi.ts"
  },
  "dependencies": {
    "@0xhoneyjar/quests-protocol": "workspace:*",
    "@0xhoneyjar/freeside-activities-adapters": "workspace:*",
    "@0xhoneyjar/quests-engine": "workspace:*"
  },
  "peerDependencies": {
    "effect": "^3.10.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "effect": "^3.10.0",
    "pg": "^8.21.0",
    "typescript": "^5.7.0"
  }
}
